---
title:     set_trace_func
subtitle:  veni, vidi, didici
site:      tracefunc.com
author:    Jamie Macey
email:     jamie at tracefunc.com
extension: xml
layout:    nil
dirty:     true
filter:    erb
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
 
  <title><%= h(@page.title) %></title>
  <subtitle><%= h(@page.subtitle) %></subtitle>
  <link href="http://<%= @page.site %>/blog/" rel="self" />
  <link href="http://<%= @page.site %>/" />
  <updated><%= Time.now.xmlschema %></updated>
  <author>
    <name><%= h(@page.author) %></name>
    <email><%= h(@page.email) %></email>
  </author>
  <id>http://<%= @page.site %>/</id>
  <% @pages.find(:limit => 10,
                 :in_directory => @page.dir,
                 :recursive => true,
                 :sort_by => 'created_at',
                 :reverse => true).each do |article|
    next if article == @page
    atime = Time.parse(article.created_at)
  %>
  <entry>
    <title><%= h(article.title) %></title>
    <link href="<%= article.url %>" />
    <id>tag:<%= @page.site %>,<%= atime.strftime('%Y-%m-%d') %>:<%= atime.to_i %></id>
    <updated><%= atime.xmlschema %></updated>
    <content type="html"><%= h(render(article)) %></content>
  </entry>
  <% end %>
</feed>

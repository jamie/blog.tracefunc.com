<div class="font-sans">
  <h2><%= link_to article.title, article %></h2>
  <p class="text-sm md:text-base font-normal text-gray-600">
    <%= format_date article.date %>
    <% if article.tags %>
      | Tags:
      <% for tag in article.tags %>
        <a href="/tags/#<%= tag %>"><%= tag %></a>
      <% end %>
    <% end %>

  </p>
</div>

<% if defined?(summary) %>
<%= article.summary(500) %>
<p><a><%= link_to "&raquo; Read more...", article %></a></p>
<% else %>
<%= article.body %>
<% end %>

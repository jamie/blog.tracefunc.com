---
layout: page
title: Archives by Date
---
<div id="articles">
  <table class="table-auto border-b">
    <% year = nil %>
    <% collections.posts.resources.reverse.each_with_index do |entry, i| %>
      <tr>
        <% if entry.date.year != year %>
          <% year = entry.date.year %>
          <th class="border-t border-l px-4 py-2"><%= year %></th>
        <% else %>
          <th class="border-l">&nbsp;</td>
        <% end %>
        <td class="border px-4 py-2"><%= entry.date.strftime('%b %e') %></td>
        <td class="border px-4 py-2"><a href="<%= entry.relative_url %>"><%= entry.data['title'] %></a></td>
      </tr>
    <% end %>
  </table>
</div>

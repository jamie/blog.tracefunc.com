<% @title = "Archives by Date" %>
<div id="articles">
  <table>
    <% year = nil %>
    <% archives.each_with_index do |entry, i| %>
      <% if entry[:date].year != year %>
        <% year = entry[:date].year %>
        <tr><th><%= year %></th><th>&nbsp;</th></tr>
      <% end %>
      
      <tr><td><%= entry[:date].strftime('%b %e') %></td>
          <td><a href="<%= entry.path %>"><%= entry.title %></td></tr>
    <% end %>
  </table>
</div>
